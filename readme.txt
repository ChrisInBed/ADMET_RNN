packages needed in environment:
    pytorch
    numpy
    pandas
    sklearn
    rdkit

rdkit can be installed automatically by processing config.py, if not successful, please input the command below in your terminal:
    conda install -c rdkit rdkit